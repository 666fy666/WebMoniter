# 截图目录

构建文档站时，`.github/workflows/docs.yml` 会将 `web/static/*.png`、`web/static/*.jpg` 同步到此目录。

若本地预览时图片缺失，可将截图放入 `web/static/` 目录（如 `配置管理.png`、`AI助手功能介绍.png`、`企业微信查询数据1.jpg` 等），或直接放入本目录后执行 `uv run mkdocs serve` 预览。
